import React, { useEffect } from "react";

const UseTest = () => {
  //   alert("now playing");
  document.addEventListener("mousedown", () => {
    alert("테스fasdf");
  });
};

export default UseTest;
